# Add this to your existing nginx configuration
# File: /etc/nginx/sites-available/recognitionbe.quanbyit.com

# Add these location blocks to your existing server block:

    # NFC Contact App routes
    location /nfc/ {
        proxy_pass http://127.0.0.1:5002/nfc/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        
        # Handle CORS if needed
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
        add_header Access-Control-Allow-Headers "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range";
    }

# Complete example server block:
server {
    listen 80;
    listen 443 ssl;
    server_name recognitionbe.quanbyit.com;
    
    # Your existing SSL configuration...
    
    # Face Recognition API (existing - keep as is)
    location / {
        proxy_pass http://127.0.0.1:5000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # NEW: NFC Contact App
    location /nfc/ {
        proxy_pass http://127.0.0.1:5002/nfc/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}